{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.11.6",
    "lerna": "^3.18.3",
    "typescript": "^3.6.4"
  },
  "scripts": {
    "publish": "lerna run clean && lerna run tsc && lerna publish"
  }
}
