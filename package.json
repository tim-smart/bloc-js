{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.0.2",
    "lerna": "^3.14.1",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "publish": "lerna run clean && lerna run tsc && lerna publish"
  }
}
