{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.14.34",
    "lerna": "^4.0.0",
    "prettier": "^2.2.1",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "publish": "lerna run clean && lerna run tsc && lerna publish"
  },
  "workspaces": [
    "packages/*"
  ]
}
